name: CI/CD Production Deploy

on:
  push:
    branches:
      - main

jobs:
  detect-changes:
    name: Detect Service Changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      backend-spring: ${{ steps.filter.outputs.backend-spring }}
      frontend: ${{ steps.filter.outputs.frontend }}
      notification-service: ${{ steps.filter.outputs.notification-service }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check path filters
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend-spring:
              - 'backend/**'
              - '.github/workflows/deploy.yml'
              - 'docker-compose.prod.yml'
            frontend:
              - 'frontend/**'
              - '.github/workflows/deploy.yml'
              - 'docker-compose.prod.yml'
            notification-service:
              - 'notifications/**'
              - '.github/workflows/deploy.yml'
              - 'docker-compose.infra.yml'

  build-and-push:
    name: Build & Push Microservices
    needs: detect-changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        service:
          - name: backend-spring
            context: ./backend
            file: ./backend/Dockerfile
            key: backend-spring
          - name: notification-service
            context: ./notifications
            file: ./notifications/Dockerfile
            key: notification-service
          - name: frontend
            context: ./frontend
            file: ./frontend/Dockerfile
            key: frontend
    steps:
      - name: Checkout repository
        if: ${{ needs.detect-changes.outputs[matrix.service.key] == 'true' }}
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        if: ${{ needs.detect-changes.outputs[matrix.service.key] == 'true' }}
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: ${{ needs.detect-changes.outputs[matrix.service.key] == 'true' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Downcase repository owner
        if: ${{ needs.detect-changes.outputs[matrix.service.key] == 'true' }}
        id: repository_owner
        run: echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        if: ${{ needs.detect-changes.outputs[matrix.service.key] == 'true' }}
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.file }}
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL || vars.NEXT_PUBLIC_API_URL || 'https://api.equibiomedic.co' }}
            NEXT_FRONTEND_URL=${{ secrets.NEXT_FRONTEND_URL || vars.NEXT_FRONTEND_URL || 'https://gm2.equibiomedic.co' }}
          tags: |
            ghcr.io/${{ steps.repository_owner.outputs.owner }}/gm2/${{ matrix.service.name }}:latest
            ghcr.io/${{ steps.repository_owner.outputs.owner }}/gm2/${{ matrix.service.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: VPS Deploy
    needs: [detect-changes, build-and-push]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy docker-compose files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: '181.205.220.123'
          username: 'server'
          password: ${{ secrets.VPS_PASSWORD }}
          port: 5434
          source: "docker-compose.prod.yml"
          target: "/media/backup/server/GM2/"

      - name: Deploy PROD to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: '181.205.220.123'
          username: 'server'
          password: ${{ secrets.VPS_PASSWORD }}
          port: 5434
          script: |
            cd /media/backup/server/GM2/ || exit 1

            # Log into ghcr.io to pull private images
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo '${{ secrets.VPS_PASSWORD }}' | sudo -S docker-compose -f docker-compose.prod.yml pull
            echo '${{ secrets.VPS_PASSWORD }}' | sudo -S docker-compose -f docker-compose.prod.yml up -d
            
            echo '${{ secrets.VPS_PASSWORD }}' | sudo -S docker system prune -f

      - name: Send notification email (prod)
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: mail.tornillosprotor.com
          server_port: 465
          username: pruebas@tornillosprotor.com
          password: Elian2020#
          subject: "Deploy GM2 - PRODUCCION"
          to: egbmaster2007@gmail.com
          from: pruebas@tornillosprotor.com
          body: "¡Se ha desplegado la actualización de PRODUCCIÓN de GM2 correctamente!"
          secure: true
